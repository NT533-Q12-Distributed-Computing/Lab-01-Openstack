#cloud-config
package_update: true
packages:
  - quagga
  - net-tools
  - iproute2
runcmd:
  - sysctl -w net.ipv4.ip_forward=1
  - echo "net.ipv4.ip_forward=1" >> /etc/sysctl.conf
  - systemctl enable zebra || true
  - systemctl start zebra || true
  - echo "Cloud-init basic routing setup done"
